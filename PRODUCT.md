# Product

<!-- impeccable:product-schema 1 -->

## Platform

web

## Users

- 主要用户：开发者本人，三十多岁上班族、有小孩。手机上一天打开三四次，每次约半分钟：记一笔刚吃的、看一眼今天还能吃多少、或看晚餐推荐。使用场景是饭桌边、外卖下单前、睡前称重后。
- 次要用户：家人（伴侣、父母）偶尔借用，走「特殊人群模式」（孕期/哺乳期/高血压/糖尿病/脂肪肝/痛风/老年人）。他们不是日常用户，界面不必为他们重排，但字号与对比不能成为门槛。
- 已确认（2026-09-04）：以自用为主设计，家人是次要入口。

## Product Purpose

个人版饮食管家：按身体数据算出每日热量与宏量目标，按规则推荐三餐，录餐后依据近 7 天真实吃法调整推荐，并给出结构性建议。成功的样子：每天少于两分钟的操作就能知道「今天还能吃多少」「晚上该吃什么」，一周后能看到日均与目标的差距在收窄。

## Positioning

- 菜品营养不是手填的，而是由「食材配比」推导：234 条食材、333 道菜（中式家常为主，覆盖西式、外卖、便利店）。这是本地、可校验、可解释的数据，不是黑箱估算。
- 推荐与分析全部是本地规则引擎，能说清「为什么推这道」（近期蛋白偏低、盐偏多、午餐连续外卖等）。贴着用户实际吃法调整，而不是硬塞健康餐。
- 纯前端、单文件、本地存储，没有账号、没有后端、没有社区。唯一可选联网能力是「说一句话录餐」，用户自备 API key 浏览器直连 Claude 或 DeepSeek。

## Operating Context

- 移动优先的 Web 应用，单手竖屏使用，也会在桌面浏览器打开。构建产物是一个 HTML 文件（`release/饮食日记.html`），双击即用。
- 底部四页签：今日 / 推荐 / 分析 / 我的。录餐面板是自底部拉起的抽屉；右下角悬浮加号是最高频入口。
- 数据全在本机 localStorage，可导出导入 JSON；导出不含 API key。
- 数值语言：千卡、克、毫克、份（蔬菜 1 份 = 100 g）、NRV 式的「摄入 / 目标」对照。所有数值标注为估算。

## Capabilities and Constraints

- 目标计算：有体脂率用 Katch-McArdle，否则 Mifflin-St Jeor；减脂 −18% 且有下限；增肌 +10%；低碳风格碳水 25%。
- 推荐：按饮食风格模板选菜、按目标收敛份量、避重、贴近实际吃法；近 7 天 ≥ 2 个完整记录日才启用调整信号；午餐连续 3 天外卖则从外卖里挑清淡的。
- 分析：近 7 天热量柱状图、近 4 周达标日历、日均 vs 目标、结构建议；自适应 TDEE（≥10 记录日 + 体重跨度 ≥10 天）。
- 特殊人群模式改动目标、推荐排除加权与建议三处；高血压模式对单菜钠 >700 mg 硬排除。
- 技术：Vite 8 + React 18 + TypeScript，vitest 49 个测试，vite-plugin-singlefile 单文件构建；无第三方 UI 库；SVG 图表手写。
- 未决：PWA 离线、拍照录餐、周购物清单、低碳模式下 ¼ 份主食显示。

## Brand Commitments

- 名称「饮食日记」。语气：营养师式的口语建议，说人话、不说教（如「还可以吃 833 千卡」「晚点清淡些」）。
- 结构约束（2026-09-04 确认）：保留底部四页签结构；保持浅色为主。其余配色、字体、布局放开。
- 现有瓷白 + 翡翠绿体系不是承诺，是证据：可以整体替换。

## Evidence on Hand

- 真实数据：`src/data/ingredients.ts` + `ingredientsUsda.ts`（364 食材，其中 130 条来自 USDA FoodData Central SR Legacy，公有领域，带 fdcId）、`src/data/dishes/*.ts`（564 菜），每道菜有份量描述、食材构成、烹法（清淡/常规/重油/油炸）与菜系标签。
- 血压 / 血糖记录（高血压、糖尿病模式）存在本机 `vitals`，分级口径写在 `src/ui/Analysis.tsx`，仅用于看趋势。
- 真实文案：规则引擎产出的调整信号与建议文案（`src/core/analysis.ts`、`src/core/planner.ts`）。
- 没有的东西，不能编造：菜品照片、用户评价、临床数据、品牌 logo。

## Product Principles

1. 半分钟完成一次操作：录餐、看余量、看推荐都在一屏内。
2. 数字要可对照：每个摄入值旁边永远有它的目标，超出要一眼可见。
3. 解释优先于说服：推荐给理由，建议给做法，不用情绪化文案。
4. 贴着真实吃法：外卖、便利店、家常菜同等对待，不做「健康餐」审美歧视。
5. 本地、可校验、可导出：不引入需要账号或后端的设计。

## Accessibility & Inclusion

- 家人（含老年人模式）会借用：正文不小于 15px，关键数值有颜色之外的文字或符号标识，触控目标 ≥ 40px。
- 深色模式已有 token，可保留，但产品以浅色为主。

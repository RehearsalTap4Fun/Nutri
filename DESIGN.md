---
name: 饮食日记
description: 一天的饮食是一张动物园导览图；米色纸底是水，苹果绿平涂是陆地，余量是陆地中间的湖。
colors:
  water: "#F5F1E4"
  shore-white: "#FFFFFF"
  shallows: "#ECE6D3"
  land: "#8ED462"
  land-light: "#C6EAA8"
  sun: "#FFD23F"
  coral-coast: "#FF6B5A"
  ink: "#2C2E2A"
  ink-2: "#5A5D58"
  muted: "#6B6E68"
  faint: "#9C9F97"
  stroke: "rgba(44, 46, 42, .92)"
  hair: "rgba(44, 46, 42, .14)"
  axis: "rgba(44, 46, 42, .38)"
  accent-green: "#2E7A1F"
  ring: "#6CBF3E"
  ring-track: "#E4DEC8"
  protein: "#E45A3F"
  protein-soft: "#F9D8D0"
  protein-text: "#B8422C"
  fat: "#3D8FD6"
  fat-soft: "#D3E6F7"
  fat-text: "#23629E"
  carbs: "#B8780A"
  carbs-soft: "#F4E4BF"
  carbs-text: "#7A5208"
  fiber: "#6E8F3A"
  fiber-soft: "#E3EBCF"
  bad: "#C8452F"
  bad-text: "#B83A26"
  warn-text: "#7A5208"
  lobe-ink: "#2C2E2A"
  lobe-ink-sun: "#6B5410"
  lobe-ink-land: "#2F4A22"
  lobe-ink-land2: "#3E5A2A"
typography:
  display:
    fontFamily: "-apple-system, PingFang SC, HarmonyOS Sans SC, MiSans, Microsoft YaHei, system-ui, Segoe UI, sans-serif"
    fontSize: "38px"
    fontWeight: 700
    lineHeight: 1.1
    letterSpacing: "-0.02em"
  headline:
    fontFamily: "-apple-system, PingFang SC, HarmonyOS Sans SC, MiSans, Microsoft YaHei, system-ui, Segoe UI, sans-serif"
    fontSize: "28px"
    fontWeight: 700
    lineHeight: 1.05
    letterSpacing: "-0.01em"
  title:
    fontFamily: "-apple-system, PingFang SC, HarmonyOS Sans SC, MiSans, Microsoft YaHei, system-ui, Segoe UI, sans-serif"
    fontSize: "17px"
    fontWeight: 700
    lineHeight: 1.05
    letterSpacing: "0"
  body:
    fontFamily: "-apple-system, PingFang SC, HarmonyOS Sans SC, MiSans, Microsoft YaHei, system-ui, Segoe UI, sans-serif"
    fontSize: "15px"
    fontWeight: 500
    lineHeight: 1.55
    letterSpacing: "normal"
  label:
    fontFamily: "-apple-system, PingFang SC, HarmonyOS Sans SC, MiSans, Microsoft YaHei, system-ui, Segoe UI, sans-serif"
    fontSize: "13px"
    fontWeight: 600
    lineHeight: 1.2
    letterSpacing: "normal"
  caption:
    fontFamily: "-apple-system, PingFang SC, HarmonyOS Sans SC, MiSans, Microsoft YaHei, system-ui, Segoe UI, sans-serif"
    fontSize: "12px"
    fontWeight: 600
    lineHeight: 1.5
    letterSpacing: "normal"
  display-compact:
    fontFamily: "-apple-system, PingFang SC, HarmonyOS Sans SC, MiSans, Microsoft YaHei, system-ui, Segoe UI, sans-serif"
    fontSize: "34px"
    fontWeight: 700
    lineHeight: 1.1
    letterSpacing: "-0.02em"
  hero-legacy:
    fontFamily: "-apple-system, PingFang SC, HarmonyOS Sans SC, MiSans, Microsoft YaHei, system-ui, Segoe UI, sans-serif"
    fontSize: "44px"
    fontWeight: 700
    lineHeight: 1
    letterSpacing: "-0.02em"
  sheet-title:
    fontFamily: "-apple-system, PingFang SC, HarmonyOS Sans SC, MiSans, Microsoft YaHei, system-ui, Segoe UI, sans-serif"
    fontSize: "20px"
    fontWeight: 700
    lineHeight: 1.05
    letterSpacing: "-0.01em"
  datenav-arrow:
    fontFamily: "-apple-system, PingFang SC, HarmonyOS Sans SC, MiSans, Microsoft YaHei, system-ui, Segoe UI, sans-serif"
    fontSize: "18px"
    fontWeight: 500
    lineHeight: 1
    letterSpacing: "normal"
  input:
    fontFamily: "-apple-system, PingFang SC, HarmonyOS Sans SC, MiSans, Microsoft YaHei, system-ui, Segoe UI, sans-serif"
    fontSize: "16px"
    fontWeight: 500
    lineHeight: 1.55
    letterSpacing: "normal"
  button:
    fontFamily: "-apple-system, PingFang SC, HarmonyOS Sans SC, MiSans, Microsoft YaHei, system-ui, Segoe UI, sans-serif"
    fontSize: "14px"
    fontWeight: 600
    lineHeight: 1.2
    letterSpacing: "normal"
  nav:
    fontFamily: "-apple-system, PingFang SC, HarmonyOS Sans SC, MiSans, Microsoft YaHei, system-ui, Segoe UI, sans-serif"
    fontSize: "11px"
    fontWeight: 600
    lineHeight: 1.2
    letterSpacing: "normal"
  mono:
    fontFamily: "ui-monospace, Menlo, monospace"
    fontSize: "12px"
    fontWeight: 500
    lineHeight: 1.55
    letterSpacing: "normal"
rounded:
  none: "0px"
  lobe: "34px 52px 44px 60px / 48px 36px 58px 40px"
  lobe-2: "56px 36px 50px 40px / 38px 54px 40px 56px"
  btn: "22px 20px 24px 19px / 20px 24px 19px 22px"
  chip: "16px 14px 17px 13px / 14px 17px 13px 16px"
  pill: "11px 9px 12px 8px / 9px 12px 8px 11px"
  bar: "34px 30px 36px 28px / 30px 36px 28px 34px"
  textarea: "18px"
  sheet-top: "44px 64px 0 0 / 30px 44px 0 0"
  capsule: "999px"
  fab: "30px 26px 32px 28px / 28px 32px 26px 30px"
  sheet-card: "24px 30px 26px 32px / 28px 24px 32px 26px"
  onboard-card: "28px 36px 30px 40px / 34px 28px 40px 30px"
  onboard-card-2: "40px 28px 36px 30px / 30px 40px 28px 36px"
  note: "18px 22px 20px 24px / 22px 18px 24px 20px"
  lake: "63% 37% 56% 44% / 46% 66% 34% 54%"
  cal-dot: "50% 45% 55% 50% / 50% 55% 45% 50%"
  empty-icon: "55% 45% 50% 50% / 50% 60% 40% 50%"
  tap-row: "16px"
  focus: "6px"
  handle: "3px"
spacing:
  xs: "2px"
  sm: "6px"
  md: "8px"
  lg: "10px"
  xl: "12px"
  shell: "14px"
  lobe-x: "20px"
  lobe-y: "18px"
components:
  button-outline:
    backgroundColor: "transparent"
    textColor: "{colors.ink}"
    rounded: "{rounded.btn}"
    padding: "10px 16px"
    typography: "{typography.label}"
  button-outline-hover:
    backgroundColor: "{colors.shallows}"
  button-primary:
    backgroundColor: "{colors.sun}"
    textColor: "{colors.ink}"
    rounded: "{rounded.btn}"
    padding: "10px 32px 10px 16px"
    typography: "{typography.label}"
  button-ghost:
    backgroundColor: "transparent"
    textColor: "{colors.accent-green}"
    rounded: "{rounded.btn}"
    padding: "6px 8px"
  button-danger:
    backgroundColor: "transparent"
    textColor: "{colors.bad-text}"
    rounded: "{rounded.btn}"
    padding: "10px 16px"
  chip:
    backgroundColor: "transparent"
    textColor: "{colors.ink}"
    rounded: "{rounded.chip}"
    padding: "6px 13px"
    typography: "{typography.label}"
  chip-on:
    backgroundColor: "{colors.land}"
    textColor: "{colors.ink}"
    rounded: "{rounded.chip}"
    padding: "6px 13px"
  pill:
    backgroundColor: "{colors.shallows}"
    textColor: "{colors.ink-2}"
    rounded: "{rounded.pill}"
    padding: "2px 9px"
    typography: "{typography.caption}"
  pill-good:
    backgroundColor: "{colors.land-light}"
    textColor: "{colors.ink}"
    rounded: "{rounded.pill}"
    padding: "2px 9px"
  pill-warn:
    backgroundColor: "{colors.sun}"
    textColor: "{colors.ink}"
    rounded: "{rounded.pill}"
    padding: "2px 9px"
  pill-bad:
    backgroundColor: "{colors.protein-soft}"
    textColor: "{colors.bad-text}"
    rounded: "{rounded.pill}"
    padding: "2px 9px"
  section:
    backgroundColor: "transparent"
    textColor: "{colors.ink}"
    rounded: "{rounded.none}"
    padding: "14px 0 6px"
  lobe-land:
    backgroundColor: "{colors.land}"
    textColor: "{colors.lobe-ink}"
    rounded: "{rounded.lobe}"
    padding: "18px 20px 16px"
  lobe-sun:
    backgroundColor: "{colors.sun}"
    textColor: "{colors.lobe-ink}"
    rounded: "{rounded.lobe}"
    padding: "18px 20px 16px"
  lobe-land-light:
    backgroundColor: "{colors.land-light}"
    textColor: "{colors.lobe-ink}"
    rounded: "{rounded.lobe}"
    padding: "18px 20px 16px"
  lobe-shore:
    backgroundColor: "{colors.shore-white}"
    textColor: "{colors.ink}"
    rounded: "{rounded.lobe}"
    padding: "14px 20px"
  input:
    backgroundColor: "{colors.shore-white}"
    textColor: "{colors.ink}"
    rounded: "{rounded.btn}"
    padding: "10px 14px"
  input-search:
    backgroundColor: "{colors.shore-white}"
    textColor: "{colors.ink}"
    rounded: "{rounded.btn}"
    padding: "12px 16px"
  nav-bar:
    backgroundColor: "{colors.water}"
    textColor: "{colors.ink-2}"
    rounded: "{rounded.bar}"
    padding: "0 6px"
    height: "62px"
  nav-item-active:
    backgroundColor: "{colors.land}"
    textColor: "{colors.ink}"
    rounded: "{rounded.chip}"
    width: "40px"
    height: "28px"
  fab:
    backgroundColor: "{colors.sun}"
    textColor: "{colors.ink}"
    rounded: "30px 26px 32px 28px / 28px 32px 26px 30px"
    width: "66px"
    height: "56px"
  sheet:
    backgroundColor: "{colors.shore-white}"
    textColor: "{colors.ink}"
    rounded: "{rounded.sheet-top}"
    padding: "10px 16px 16px"
  sheet-card:
    backgroundColor: "{colors.shallows}"
    textColor: "{colors.ink}"
    rounded: "24px 30px 26px 32px / 28px 24px 32px 26px"
    padding: "14px 16px"
  toast:
    backgroundColor: "{colors.ink}"
    textColor: "{colors.water}"
    rounded: "{rounded.capsule}"
    padding: "12px 18px"
  note:
    backgroundColor: "{colors.land-light}"
    textColor: "{colors.ink}"
    rounded: "18px 22px 20px 24px / 22px 18px 24px 20px"
    padding: "9px 14px"
---

# Design System: 饮食日记

## Overview

**Creative North Star: "动物园导览图"（Zoo & Gardens Guide Map）**

整个应用是一张平涂的园区导览图。米色纸底（`--bg`）是水面，苹果绿色地（`--land`）是陆地，今天的剩余热量是陆地中间挖出来的一个湖；吃得越多陆地越涨、湖越缩，超标时湖消失、岸线变珊瑚红。页面级区块不再是白卡片：它们是水面上的一块块地名，靠留白与地名式标题分区，没有边框、没有投影、没有渐变、没有颗粒。只有抽屉与表单是"白色岸地"，用白底把人从水面接到陆地上填东西。

密度是手机竖屏、单手、半分钟一次操作的密度：正文 15px、行高 1.55、触控目标 ≥ 40px；数字永远与目标并排出现（"987 / 1820 千卡"），超出立刻用 `--bad` 标红。控件是一种统一的"胶囊"，但边缘不是完美圆，而是四角不等的椭圆半径（`--r-btn / --r-chip / --r-pill / --r-bar`），像手绘的岸线微微起伏；主操作按钮是太阳黄并在右端带一颗墨色圆片。

已确认的视觉拒绝：同类应用的"白卡片 + 圆环 + 投影"三件套；渐变；纸张颗粒；灰色作为色地上的次级文字色（色地上的次级文字一律从该色相里调深）。

**Key Characteristics:**
- 水 / 陆 / 湖三层地理隐喻，全部用平涂色块和 SVG 岸线表达，零阴影零渐变
- 一款系统无衬线（PingFang SC 优先），字重三档 500 / 600 / 700，数字 tabular-nums
- 控件统一为"不规则胶囊"：描边 1.5px inset `--stroke`，激活态填 `--land`，主操作填 `--sun`
- 珊瑚红 / 池塘蓝 / 琥珀只做三大宏量的数据色（蛋白 / 脂肪 / 碳水），不做装饰
- 浅色为主，暗色 token 完整维护但地位次之；底部四页签结构是用户承诺
- 图表手写 SVG，与容器 1:1 像素绘制；空心虚线柱表示"没记全"，墨色虚线是目标

## Colors

一组米、绿、黄的园区平涂色，只在三条宏量条和超标态上出现红、蓝、琥珀这三个高饱和色相。

### Primary
- **陆地绿 land** (`--land`): 今日页的陆地色地、午餐 lobe、激活的 chip / seg / 导航圆片、图表柱、达标日历格。是系统里最大面积的色。
- **浅陆地 land-light** (`--land-2`): 晚餐 lobe、建议条 `.note`、`.pill.accent / .pill.good`、文本选区背景。承接大片 `--land` 之外的次级绿色面。
- **深绿文字 accent-green** (`--accent`): 唯一可作文字的绿：`.btn.ghost`、`.reason` 推荐理由、复选框 accent-color。不作填充。

### Secondary
- **太阳黄 sun** (`--sun`): 主操作（`.btn.primary`、FAB）、早餐 lobe、`.pill.warn`、未达标日历格、`.finding .ico.warn`、Toast 内的动作文字。它同时表示"最高频入口"与"偏离"，两者不会同屏冲突。
- **珊瑚岸线 coral-coast** (`--coral`): 只有一个用途：超标时今日页陆地的岸线描边（`.land.over .land-coast`，4px 圆头）。不用在文字或填充上。

### Tertiary（三大宏量数据色，各自三档：主色 / -soft 底 / -text 可读文字）
- **蛋白红 protein** (`--protein / --protein-soft / --protein-text`): 蛋白宏量条填充、条底、`.pill.bad` 的底。
- **脂肪蓝 fat** (`--fat / --fat-soft / --fat-text`): 脂肪宏量条。
- **碳水琥珀 carbs** (`--carbs / --carbs-soft / --carbs-text`): 碳水宏量条。
- **纤维橄榄 fiber** (`--fiber / --fiber-soft`): 纤维条（次级）。
- **超标红 bad** (`--bad / --bad-text`): 宏量条溢出段 `.fill.over`、Ring 超标描边、`.btn.danger` 描边与文字、超标数字。与 `--protein` 是不同 token，虽然色相相近；不要互换。

### Neutral
- **水面 water** (`--bg`): 页面底色、导航条底、Toast 与 tip 的文字色、theme-color。
- **白色岸地 shore-white** (`--surface`): 抽屉、Onboard 表单卡、输入框、加餐 / 已吃 lobe、空态图标底。
- **浅滩 shallows** (`--surface-2`): hover 底、默认 `.pill` 底、抽屉内 `.card` 底、宏量条默认轨道。
- **墨 ink** (`--ink`): 正文、标题、图表目标线与折线、今日柱、Toast 底、主按钮圆片、焦点环。
- **墨 2 / muted / faint** (`--ink-2 / --muted / --faint`): 依次为标签与次级文字、说明文字与图表刻度、最弱的占位。
- **岸线描边 stroke** (`--stroke`, 92% 墨): 所有描边控件的 1.5px inset 边。
- **细线 hair** (`--hair`, 14% 墨): 列表分隔线、图表网格、输入框静默边、已吃 lobe 的内描边。
- **轴 axis** (`--axis`, 38% 墨): 图表轴线、空心虚线柱、抽屉把手、遮罩 `rgba(44,46,42,.38)`。
- **色地文字 lobe-ink 家族** (`--lobe-ink / -sun / -land / -land2`): 色地上的正文用 `--lobe-ink`，次级文字按所在色地的色相取对应深色，而不是灰。

### Named Rules
**The Tokens-First Rule.** 所有颜色、圆角、缓动、时长都是 `src/styles.css` 顶部的 token，明暗两套；组件只引用 token；改视觉先改 token，不在组件规则里写裸值。

**The Three-Macro Rule.** `--protein`（红）/ `--fat`（蓝）/ `--carbs`（琥珀）三个色相分离，经 WCAG 对比度校验；琥珀与蓝只表示碳水与脂肪，不做别的语义。餐次的颜色来自 lobe 色（早餐 sun / 午餐 land / 晚餐 land-2 / 加餐 surface），与宏量色互不借用。

**The Ghost-On-Lobe Rule.** 色地与陆地上的幽灵按钮（`.btn.ghost`）用 `--lobe-ink`，不用 `--accent` 深绿：绿在绿上、绿在黄上都不到 4.5:1。

**The Hue-Not-Grey Rule.** 色地（lobe、今日页陆地）上的次级文字不用 `--muted` 灰，用同色相的 `--lobe-ink-*` 深色。

**The Distill Rule.** 任何超过两行的解释文字放进 Fold，正文只留一句；多个并列数字用 Stats 而不是一句话；规则引擎的提醒用 SignalChips 露标题；图形的含义用 Legend 色样而不是脚注。每屏默认可见的说明文字以"一眼能扫完"为限。

**The Sodium Rule（2026-09-07）.** 钠是按"每道家常菜 1.5~2 g 盐"估出来的，天天超上限，当日常指标只会一直红。默认不显示钠：今日页事实行、推荐合计、抽屉数据条、分析宏量条、档案 kv 都不出现，含钠数字的提醒句一并过滤（`showsSodium` / `withoutSodiumNotes`，`src/ui/format.ts`）。只有高血压模式（钠上限是核心指标）才显示。"少盐"标记与"近期盐偏多"这类做法说明照常显示。

**The Light-First Rule.** 产品以浅色为主；`prefers-color-scheme: dark` 块存在且每个 token 都有对应值，需要同步维护，但暗色是次要的，不为暗色单独设计版式。

## Typography

**Display Font:** 系统无衬线（`-apple-system, "PingFang SC", "HarmonyOS Sans SC", "MiSans", "Microsoft YaHei", system-ui, "Segoe UI", sans-serif`）
**Body Font:** 同上一款
**Label/Mono Font:** 数字继承同一款并开 `font-variant-numeric: tabular-nums`（`--font-num` 指向 `--font-ui`）；仅 JSON 导入导出 textarea 用 `ui-monospace, Menlo, monospace` 12px。

**Character:** 一款系统黑体、三档字重、无衬线无展示字体。声音是营养师的口语（"还可以吃 833 千卡"），字本身不表演；层级靠字号与字重，标题收 -0.01em、大数字收 -0.02em。

### Hierarchy
- **Display**（700, 38px, 1.1, -0.02em）: 湖中的余量数字 `.lake-num`；≤430px 缩为 34px。旧 `.hero-num` 44px 保留兼容，不再新用。
- **Headline**（700, 28px, 1.05, -0.01em）: 页面 h1（今日 / 推荐 / 分析 / 我的、Onboard 标题）。
- **Title**（700, 17px, 1.05）: 区块标题 h2（地名）；抽屉标题 h2 提到 20px，抽屉内 `.card h2` 16px；h3 与 `.slot-head` 15px 700。
- **Body**（500, 15px, 1.55）: 全站正文默认，也是可接受的最小正文（家人借用的可读下限）。
- **Label**（600, 13-14px, 1.2）: 按钮 14px、chip / seg / 字段 label / 宏量条 13px。
- **Caption**（600, 12px）: pill、`.tiny`、图表说明、`.reason`、`.hero-sub`、Toast 提示 tip；导航文字 11px 600；图表刻度 11px 600 填 `--muted`。

### Named Rules
**The Three-Weights Rule.** 只用 500（正文）/ 600（标签、按钮、次级强调）/ 700（标题、数字、`.reason` 在色地上）。不引入 400 或 800。

**The Paired-Number Rule.** 每个摄入数字旁边都有它的目标（"45 / 127 g"），数字开 tabular-nums；超出时数字改用 `--bad-text`，不靠颜色单独传达（湖里同时写"已超出"）。

## Layout

单列、移动优先。`.app` 最大宽 640px 水平居中，左右内边距 14px，桌面 1440 也保持这一列（两侧全是水面）。底部预留 `--nav-h`(74px) + FAB 高度 + safe-area，避免内容被导航条和 FAB 遮住。顶栏透明，高 56px + safe-area-top，左标题右日期胶囊。

今日页的陆地 `.land` 用负外边距出血到视口两侧（`margin: 0 calc(50% - 50vw)`），并向上顶到顶栏之下；岸线是一个 `viewBox 0 0 100 100`、`preserveAspectRatio="none"` 的 SVG 路径从左上涨到右下出画。陆地内部是 `188px 1fr` 两列（湖 + 事实），≤430px 变 `156px 1fr`，≤359px 变单列；湖的尺寸按余量比例 `--lake-scale` 在 0.58-1 之间缩放。

节奏：区块 `.card` 上下 14px / 6px、区块间 10px；lobe 间 12px，lobe 相对页面向外溢出 4px；`.stack` 10px、`.row` 8px、chips 6px、列表条目上下 11px、`.finding` 与 `.kv` 用 `--hair` 1px 顶线分隔。表单用 `.grid2 / .grid3`（10px / 8px 间距）。日历为 7 列 28px 格、8px 间距。

抽屉 `.sheet` 从底部涨起，最大高度 92dvh、宽 640px，背后 `.app` 缩到 .965 并降到 85% 不透明度。

## Elevation & Depth

没有阴影。`--shadow-card` 与 `--shadow-float` 都是 `none`，所有 `box-shadow` 声明只用来画 inset 描边（1.5px `--stroke` 或 `--hair`，焦点 2px `--ink`）。深度由三层材质表达：水面（`--bg`）上放平涂陆地（`--land` 等），陆地上挖湖（`--lake` = `--bg`），要"离开水面填表"时换到白色岸地（`--surface`），岸地里再放一层浅滩（`--surface-2`）。抽屉打开时靠 38% 墨色遮罩加缩放主页面表达前后关系，而不是投影。

### Named Rules
**The No-Shadow Rule.** 不加 `box-shadow` 外投影、`filter: drop-shadow`、渐变或颗粒纹理；一切层级用平涂色块和 1.5px inset 描边。（主按钮 hover 的 `brightness(1.04)` 是唯一 filter。）

**The Coast-Only-Motion Rule.** 会动的只有岸线：陆地填充、湖缩放、抽屉涨起、FAB、导航圆片、Toast 用 `--ease-coast`（`cubic-bezier(.34,1.45,.64,1)`，一次过冲）配 `--t-coast` 400ms；其余状态切换用 `--ease-out` 配 `--t-fast` 150ms / `--t-med` 220ms；`prefers-reduced-motion` 下全部压到 1ms。

## Shapes

两套形体语言并存，各有分工：

- **地形（大块）**：lobe、Onboard 卡、抽屉内卡、`.note`、FAB、空态图标都用四角不等的双值椭圆半径（如 `--r-lobe`、`--r-lobe-2`），相邻 lobe / 卡片用 `:nth-of-type(even)` 交替两套半径，让一列色地看起来像不同形状的园区。抽屉顶部 `44px 64px 0 0 / 30px 44px 0 0`。日历格是 `50% 45% 55% 50% / 50% 55% 45% 50%` 的歪圆。
- **岸线胶囊（控件）**：按钮、seg、stepper、输入框用 `--r-btn`；chip、seg 内钮、导航圆片、日期胶囊用 `--r-chip`；pill 用 `--r-pill`；导航条用 `--r-bar`。都是"接近胶囊但四角不等"，差值 2-4px。textarea 用 18px 均匀圆角。
- **页面区块**：`.card` 在页面级是 `--r-card: 0px` 且透明，无形体。

描边一律 1.5px inset `--stroke`，激活态去描边改填色。仍在用 `999px` 真胶囊的元素：`.toast`、`.tip`、`.track / .fill`、`.datenav button`、`.nav button`、`.toast-action`、以及全局 `:focus-visible` 的 outline 圆角。这是构建的现状，记录如此；未统一到 `--r-*` 家族。

## Components

### Buttons
- **Shape:** 岸线胶囊（`--r-btn`），字 14px 600，不换行；`.sm` 6px 12px / 13px。
- **Outline（默认 `.btn`）:** 透明底，1.5px inset `--stroke`，文字 `--ink`；hover 填 `--surface-2`；active `scale(.97)` 150ms。色地上描边与文字换成 `--lobe-ink`。
- **Primary（`.btn.primary`）:** `--sun` 底、`--ink` 字、无描边、右侧留 32px 放一颗 11px 墨色圆片（`::after`，`.sm` 为 9px）；hover `brightness(1.04)`。在 `--land` 色地上反转为 `--ink` 底 `--bg` 字、圆片 `--bg`；在 `--land-2` / 白色 lobe 上仍为 `--sun`。
- **Ghost（`.btn.ghost`）:** 无底无描边，`--accent` 深绿文字，6px 8px；hover 下划线（offset 3px）。
- **Danger（`.btn.danger`）:** 描边 `--bad`、文字 `--bad-text`。
- **Disabled:** 45% 不透明。
- **FAB:** 66×56 的歪胶囊，`--sun` 底，26px 2.2px 线条加号，固定右下、贴 640px 容器右缘 16px；hover 1.04、active .94，`--ease-coast`。

### Chips
- **Style:** 透明底 + 1.5px inset `--stroke`，13px 600，`--r-chip`；横向滚动行 6px 间距、隐藏滚动条。
- **State:** `.on` 填 `--land`、去描边、文字 `--ink`；active `scale(.96)`。陆地上的 chip 描边 `--lobe-ink`，`.on` 反转为 `--lobe-ink` 底 `--land` 字。

### Pills（状态标签）
- 12px 600、2px 9px、`--r-pill`。默认 `--surface-2` / `--ink-2`；`.accent` 与 `.good` 是 `--land-2` / `--ink`；`.warn` 是 `--sun` / `--ink`；`.bad` 是 `--protein-soft` / `--bad-text`。色地上改为 55% 白底。

### Sections / Lobes / Cards
- **页面级 `.card`:** 透明、无圆角、无边框、`14px 0 6px`，仅用 h2 地名分区，区块间 10px。
- **Lobe（`.lobe lobe-<slot>`）:** 平涂色地，`--r-lobe` / 偶数 `--r-lobe-2`，`18px 20px 16px`，向外溢出 4px。早餐 `--sun`、午餐 `--land`、晚餐 `--land-2`、加餐与已吃白色 `--surface`（已吃另加 1.5px `--hair` 内描边、`14px 20px`）。内部所有 muted / ink2 文字换 `--lobe-ink-*`，分隔线 18% 墨。
- **抽屉内 `.card`:** `--surface-2` 浅滩底，歪圆角 `24px 30px 26px 32px / 28px 24px 32px 26px`，`14px 16px`。
- **Onboard `.card`:** `--surface` 白底，`28px 36px 30px 40px / 34px 28px 40px 30px`（偶数交替），`16px 18px`。
- **`.note`:** `--land-2` 底 13px 建议条，歪圆角。

### Inputs / Fields
- **Style:** 白底 `--surface`，静默态 1.5px inset `--hair`，`--r-btn`，`10px 14px`；label 13px 600 `--ink-2`，上下 5px。搜索框描边加重为 `--stroke`、`12px 16px`、16px 字。textarea 18px 圆角、16px 字、`resize: vertical`。
- **Focus:** 去 outline，描边变 2px `--ink`。全局 `:focus-visible` 为 2px `--ink` 外描边、offset 2px。
- **Seg（分段）:** 外壳 `--r-btn` 描边、3px 内边距、2px 间距；钮 `--r-chip`、13px 600 `--ink-2`；`.on` 填 `--land` 字 `--ink`。
- **Stepper:** `--r-btn` 描边外壳，40×40 按钮 20px 字，中间值 64px 700 tabular。

### Navigation
- 固定底部。整条 `.nav` 是实底的水面带（`--bg`，上 6px 下 10px 内边距 + 安全区），顶边由一个 12px 高、`fill: var(--bg)` 的 SVG 小岸线 `.nav-shore` 收边：内容滚到底是滑到岸下被盖住，不透在胶囊后面。带内是 62px 高的描边胶囊（`--r-bar`，1.5px `--stroke`），最大宽 640px，四等分。`--nav-h` 86px 是岸线加导航带的总高，页面底部留白、悬浮加号、Toast 都以它定位。（2026-09-07 由「透明浮层」改为实底带，用户反馈内容与胶囊重叠显得乱。）
- 每项：22px 1.75px 圆头线条 SVG 图标（碗 / 叶 / 柱状图 / 人）+ 11px 600 文字，默认 `--ink-2`。
- 激活：图标外 40×28 的 `--r-chip` 圆片填 `--land`、`scale(1.06)`（`--ease-coast`），文字变 `--ink`。按下图标 `scale(.92)`。
- 顶栏日期：左右 `‹ ›` 18px 透明按钮（hover 浅滩底），中间"今天"14px 600 描边胶囊（`--r-chip`）。

### Land & Lake（签名组件，今日页）
出血到视口的 `--land` 色地，下缘由 SVG 路径 `.land-fill` 画出从左上到右下的岸线；中间一个歪椭圆湖（`border-radius: 63% 37% 56% 44% / 46% 66% 34% 54%`，旋转 -7°，`--lake` 底）写着"还可以吃 / 833 / 千卡"，按余量比例缩放。右侧是已吃合计与蔬果纤维钠事实；下方三条宏量条（轨道 55% 白、填充为宏量色、溢出段 `--bad`）。超标时加 `.over`：湖透明度归零、岸线 `.land-coast` 以 `--coral` 4px 圆头描边显现、湖文字换 `--lobe-ink`。

### Charts
手写 SVG，`viewBox` 宽度由容器实测得到，1:1 像素绘制，文字不随缩放变形。柱 `--land`、今日柱 `--ink`、**空心 3 3 虚线柱（`.bar.dim`，描边 `--axis`）= 没记全的一天**；**墨色 1.5px `5 4` 虚线 = 目标**（右上角"目标 1820"标签 700）；网格 `--hair`、轴 `--axis`、刻度 11px 600 `--muted`；折线 `--ink` 2px 圆头、点 `--land` 填 `--ink` 边。悬停 tip 是墨底 `--bg` 字的 999px 胶囊。达标日历：歪圆格，达标 `--land`、未达标 `--sun`、今天 2px `--ink` 内描边、未来 1px `--hair`。

### 提炼组件（`src/ui/bits.tsx`，2026-09-05）
长段说明不上正文，数字不塞进句子。七个小件都只引用 token：
- **Fold（折叠说明）:** `<details class="fold">`，summary 是 13px 600 `--accent` 的一行带右向 chevron，展开旋转 90°；正文 13px `--muted`。用于免责声明、费用说明、参考范围等"只在想看时才看"的文字。
- **Stats（数据条）:** `repeat(auto-fit, minmax(110px, 1fr))` 网格，每格 12px 600 `--ink-2` 标签 + 18px 700 数值，`/ 目标` 与单位 12px `--muted`；`.bad` 数值用 `--bad-text`。替代"目标 1820 千卡 · 蛋白 127 g。推荐合计…"这类一句四个数。
- **SignalChips（信号胶囊）:** 营养师提醒只露第一个逗号前的标题，`--land-2` 底 `--r-chip` 胶囊，点开变 `--land` 并在下方显示整句；一次只展开一条。
- **Legend（图例）:** 12px 色样 + 一个词：`--land` 实心、虚线空心（没记全）、`--ink` 实心（所选日期）、墨色虚线段（目标）。替代解释图形含义的整句脚注。
- **Bullets（图标要点）:** `24px 1fr` 网格，左侧 24px `--land-2` 圆底 1.75px 线条图标，右侧 13px `--ink-2`。两三条，替代整段功能说明。
- **ShareBar（供能分段条）:** 10px 高三段 `--protein / --fat / --carbs` 分段条，`--r-pill` 圆角，下方三个带圆点的百分比标签；参考范围放进 Fold。
- **ProgressRow（进度行）:** `20px 1fr auto 64px` 网格：达成态 `--land` 圆底打勾、未达成 `--hair` 描边空圆，右侧 64px `--surface-2` 轨道 `--land` 填充。用于自适应消耗的三个门槛。
- **MetricRow（宏量条 + 结论标记，2026-09-07）:** `1fr 26px` 网格，宏量条右侧挂一个 24px 圆标记：warn 太阳黄底叹号、good 陆地绿底对勾、info 描边 i、没有结论则隐藏占位；点标记在条下方展开该项的标题、数据句与做法（左缩进 64px 与轨道对齐）。分析页的"结构建议"由此并入"日均 vs 目标"，归不到宏量的条目单列为"饮食习惯"。
- **推荐页数据条（2026-09-08）:** 没吃任何一餐时显示「推荐热量 / 推荐蛋白 对 目标」；已有已吃餐次时推荐只覆盖其余餐次，数据条改为「全天热量 = 已吃 + 推荐 对 目标」，值下一行 `.stat-sub` 小字写「已吃 800 + 推荐 1050」，避免把剩余餐次的量误读成推荐不够。`StatItem.sub` 为此新增。
- **偏差图 · 日均 vs 目标（2026-09-08）:** 分析页不再用「填充进度条」（那是今日页的语言：朝今天的目标推进），改用围绕目标线的偏差图。图里只有三样东西：`.var-band` 中性浅滩色（`--surface-2`）的合适区间带、`.var-zero` 居中成一列的目标线（2px `--ink`）、`.var-bar` 按结论上色的 6px 柱（达标 `--land`、要改 `--sun`、提示 `--axis`、无结论且不在区间 `--faint`），向左不足、向右超出，超出 ±60% 夹在刻度边缘加三角箭头；图里不放文字，偏差百分比是右栏 84px 的主数字（要改用 `--warn-text`），「日均/目标 单位」做 11px 小字。合适区间宽度与 `core/analysis` 的判定阈值一致（热量 +10% / −20%，孕产期 −15%；蛋白 85%、纤维 70%、蔬菜 60%、水果 100 g、饮水 70%「至少到」；脂肪超 25%、钠超 20%「不要超」）。标签圆点保留营养素识别色；达标不再放对勾图标，右侧只留要改 / 提示两种能点开的标记。第一版把绿带、底线、浮动百分比都放进了图里，用户反馈「杂乱」，据此收敛。计算在 `src/core/variance.ts`。
- **份量文案（2026-09-08）:** 整份写「1份 / 2份」，非整份一律换算成克写「约 115 g」（半份、¾份这类分数不直观），克数按成品重量 `servingGrams()` 算（优先读份量描述里的「可食 N g」「约 N g」，读不出才用食材合计），≥30 g 取到 5 的倍数。今日列表、预算面板、推荐页、录入页步进器与快捷份量、说一句话、能不能吃、toast 统一用 `portionText / entryPortionText`；自定义条目不知道一份多重，保留分数写法。「按克」输入与显示用同一个成品重量。
- **常吃胶囊（一键补记）:** 今日页空餐次下一行 `.chips.quick`：12px "常吃"灰标签 + 最多 3 个描边胶囊（12px 加号图标 + 菜名），来源按餐次分开统计（`src/core/recent.ts`）：最近 30 天在该餐次记过的菜按次数排、次数相同近期优先，不足时用适合该餐次的收藏与总榜补位，早餐常吃的包子不会出现在晚餐栏；录入页默认列表「X 餐常吃 / 收藏」同样跟着选中的餐次变；点一下按一份直接记录，toast 可撤销。空态时悬浮加号隐藏，避免双主操作。
- **理由标签:** 推荐里每道菜的整句理由收成一个 `--land-2` 底 pill（高蛋白 / 清淡 / 少油 / 加菜 / 粗粮 / 低碳 / 加水果 / 外卖优选），整句放在 title 里；同一句不再在每道菜下重复。
- **SignalChips 上限:** 默认最多露 3 个，其余折成描边 "+N" 胶囊点开；标题截断时去掉未闭合括号与尾标点。
- **Stats dense:** 抽屉里的"这一条"用紧凑数据条（`minmax(58px, 1fr)`，15px 数值），替代一句五个数。
- **餐次用分段控件:** 录餐与说一句话里的餐次从原生下拉改为四段 `.seg`，与时间输入按 `1fr 118px` 并排（`.grid2.slot-time`）。份量三套控件收成步进器 + 份量胶囊，"按克"是其中一个胶囊，点开才出现克数输入。
- **预算面板（2026-09-07）:** 今日页陆地上的「还能吃什么」胶囊（余量 >50 千卡才出现），点开在陆地下方列 6 道放得下的菜：菜名 × 份量、一句理由、蛋白、热量、加号一键记录；标题下一行「按缺口挑」列出四个主要参数（蛋白/脂肪/碳水/纤维）里已超与还差得多的项（缺口用中性 `--ink-2`，只有已超用 `--bad-text`——宏量色里的蛋白红会和警示红撞，所以这里不按宏量上色），面板标题右侧有「收起」幽灵按钮（展开时列表自动滚进视野），点一道菜记录后也自动收起。每行加号左侧有「不喜欢」叉（`.btn.ghost.quiet`，与推荐页的叉同义：进不推荐名单、toast 可撤销、我的页可恢复），点后列表立刻补上下一个候选，面板不收起（`src/core/budget.ts` 按剩余热量七成、蛋白缺口、模式加权、常吃收藏打分，油炸降权）。
- **一周视图（推荐页）:** 标题右侧 `.seg.seg-sm` 切「单日 / 一周」。一周是 7 行 `.week-row`（`64px 1fr auto` 网格：星期与日期、三餐菜名、当日热量），当前日期行 `--land-2` 底，点行回到该日单日视图；下方「全周换一换」描边胶囊与折叠的「本周采购清单（N 项）」。
- **血压 / 血糖卡（分析页，仅高血压 / 糖尿病模式）:** 字段行（收缩压 + 舒张压，或血糖 + 空腹/餐后 2h 分段）+ 太阳黄记录键；紧凑数据条给 7 天均值；两条以上时一条墨色趋势线；近 7 条记录列表，每条带 `.pill.good/.warn/.bad` 分级与灰色删除叉；尾注「以医生方案为准」。
- **扫码面板（录餐抽屉第三入口，2026-09-07）:** 分段控件「搜菜名 / 说一句话 / 扫码」。取景框是 4:3 的墨色地（`--r-lobe`），未开相机时中间一颗太阳黄「打开相机扫码」，扫描中露出画面并叠一个 `--sun` 描边的取景框与白字提示；下方永远有手输条码输入行 + 查询键、拍照识别、返回；页脚一行 Open Food Facts 归属。查到：产品名 + 品牌·规格·来源，「每 100 g」紧凑数据条，克数输入与「一份 / 100 g / 50 g」胶囊，「这一份」数据条，「再扫一个 / 选用这份」。没查到：标题 + 条码 + 一条图标要点，「再扫一个 / 按成分表录入」。
- **饮水水塘（今日页，2026-09-07）:** 一串 250 ml 的小水塘沿 8~22 点的「溪流」排开。空塘是干河床：`--surface-2` 浅滩色加 1.5px `--axis` 虚线岸，宽扁歪椭圆（相邻塘两套半径交替）；有水的塘：`--pond-deep` 底、`--pond` 水面从底部涨起（`--ease-coast` 过冲）、左上一小片 55% 白的天光反光（平涂，无渐变）。塘下 14px 处一根 `--hair` 基线把塘串起来，基线上 2×13px 的墨色刻度是「现在」；每塘下面 10px `--faint` 的应喝完时刻，亮了的变 `--fat-text` 蓝。点第几个亮到第几杯，点已亮的退回前一杯；无加减与数字输入。到点没喝的塘「口渴」：虚线岸变池塘蓝，每 2.6 秒轻轻抖一下，一滴水从上方掉进塘里摊开（按序号错开 180ms）；点满整排从左到右荡一次水波。这是这个世界里除岸线之外唯一的循环动效，只在口渴时出现，reduced-motion 下关闭。
- **条目删除（2026-09-07）:** 今日记录每条右侧一个 14px 灰色叉（`.btn.ghost.row-del`，hover 变 `--bad-text`），点了直接删，toast 可撤销；手机上整行可左滑 88px 露出右侧 76px 宽的 `--bad` 底白字"删除"胶囊（`.swipe` 外层裁切，行本身 `--bg` 底盖住后面的键），松手位移超过 44px 停在打开态，再点行关闭。删除永远可撤销，所以不做二次确认。
- **FindingRow（饮食习惯行）:** 现只用于归不到宏量的结论（外卖、早餐、夜宵、加工食品、记录太少、蛋白分布）：warn 默认展开，其余折成一行标题 + 数据句省略 + chevron。

### Sheet（录餐抽屉）
从底部 `slide-up`（`--t-coast` + `--ease-coast`）涨起的白色岸地：顶部 `44px 64px 0 0 / 30px 44px 0 0`，36×5 `--axis` 把手，`10px 16px` 内边距，内部 12px 纵向间距；遮罩 38% 墨色 fade 220ms；背后页面缩到 .965。可拖拽关闭（`.dragging` 关动画，`.settle` 用 `--t-slow` 回弹）。

### Toast
固定在导航条上方 20px 的墨色 999px 胶囊，`--bg` 文字 14px，`12px 18px`，宽 `min(92vw, 420px)`；动作文字 `--sun` 700；`toast-in` 从下 12px 升起，`--ease-coast`。

### Findings（分析建议）
`26px 1fr` 网格，24px 圆形图标底：`.good` `--land`、`.warn` `--sun`、`.info` 描边；标题 14px 700，做法 `--ink-2`；条目间 `--hair` 顶线。

## Do's and Don'ts

### Do:
- **Do** 所有新颜色、圆角、缓动、时长先加到 `src/styles.css` 顶部（`:root` 与 dark 两处），组件只写 `var(--…)`。
- **Do** 用 `--protein / --fat / --carbs` 三档表示蛋白 / 脂肪 / 碳水，每条数据色都配它的 `-soft` 轨道与 `-text` 文字色；改色相前重做 WCAG 对比度校验。
- **Do** 用 lobe 色表示餐次：早餐 `--sun`、午餐 `--land`、晚餐 `--land-2`、加餐 / 已吃 `--surface`。
- **Do** 保留共享类名 `.card .btn .pill .chip .seg .stepper .meter/.track/.fill .list-item .sheet* .toast .cal .finding`；可改样式，不可改名。
- **Do** 图表按容器宽 1:1 绘制；`.chart .bar.dim` 空心虚线柱只表示"没记全的一天"，`.chart .target` 墨色虚线只表示目标。
- **Do** 保持底部四页签、浅色为主；改动前跑 `npm test && npm run build`（56 测试，单文件产物约 638 kB）。
- **Do** 色地上的次级文字用 `--lobe-ink-*`，图标用 1.75px 圆头 `currentColor` 线条 SVG（`src/ui/icons.tsx`）。
- **Do** 每个摄入数字旁写目标，超出用 `--bad-text` 并有文字提示；正文不小于 15px，触控目标 ≥ 40px。
- **Do** 长说明进 Fold、并列数字进 Stats、提醒进 SignalChips、图形含义进 Legend（见「提炼组件」）。

### Don't:
- **Don't** 加外投影、渐变、颗粒、毛玻璃；`--shadow-card / --shadow-float` 为 `none` 是刻意的。
- **Don't** 用白卡片 + 圆环 + 投影表达今日余量；余量只在湖里出现。
- **Don't** 把琥珀或蓝用在碳水、脂肪以外的语义上，也不要把 `--protein` 当作 `--bad` 用（两者是独立 token）。
- **Don't** 在色地上用 `--muted` 灰做次级文字。
- **Don't** 引入第二款字体、展示字体或 400 / 800 字重；不引入第三方 UI 库或图标字体。
- **Don't** 为暗色单独改版式：暗色只是同一套 token 的另一组值。
- **Don't** 在组件规则里写裸色值或裸圆角（现存 `999px` 与内联 `rgba(255,255,255,.55)` 是遗留，不再新增）。
